[project]
name = "sorteros-kickoff"
version = "0.1.0"
description = "Mac-side web UI for kicking off SorterOS builds and downloads on Hive"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn>=0.32",
    "sse-starlette>=2.1",
]
